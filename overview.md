# Cloudflare 数据库迁移完成概览

## 完成内容

- 将跳绳赛事报名系统的数据访问从 Supabase 浏览器直连切换为 Cloudflare Pages Functions 同域 API。
- 创建独立 Cloudflare D1 数据库：`rope-jump-registration-d1-20260814`（ID：`2b3c1a09-f3c5-4950-9b6a-10ee858ba5aa`）。
- 在新 D1 中完成 15 张业务表、会话表、审计表、索引、外键、报名容量触发器与初始管理员迁移。
- 新增服务端认证、会话 Cookie、角色权限、结构化错误响应、个人报名状态查询和审计日志。
- 前端保持既有 Store 调用模式，通过兼容适配层转为调用 `/api`；不再内置 Supabase 项目 URL。
- 将生产构建输出到独立 `release-20260814` 目录，避免清理原有 `dist`。
- 已部署到 Cloudflare Pages 加固版本：<https://107b95b6.rope-jump-registration.pages.dev>。
- 已创建并绑定独立 R2 媒体桶：`rope-jump-registration-media-20260815`（绑定名 `REGISTRATION_MEDIA`），用于运动员头像和照片。
- 已将测试账号 `001w` 当前赛事队伍的 15 名运动员替换为虚拟生成的真人风格头像：头像以独立 320×320 JPEG 保存至 R2，头像地址已更新至 D1；未改动姓名、身份资料、报名、教练或领队数据，已核验 15/15 个头像关联成功。
- 新增计分表自助流程：管理员从当前已发布出场顺序导入独立 D1 快照；俱乐部端新增“计分表”入口，可按队伍或运动员姓名查询，自动填充项目、单位、组别、轮次/场地、姓名；多人项目按“第一名运动员等”显示，并可直接生成 A4 PDF 下载。迁移 `0004_scorecard_snapshots.sql` 已应用至专用 D1，最终版本发布预览地址为 <https://994d4eb3.rope-jump-registration.pages.dev>，正式 `/club`、`/admin` 返回 200。

## 已验证

- Pages Functions 编译通过。
- 前端 TypeScript 检查与 Vite 生产构建通过。
- 线上公开赛事查询返回 D1 数据。
- 管理员登录可创建 HTTP-only 会话。
- 未登录读取管理员数据会被正确拒绝。
- 管理端、俱乐部端、个人报名端页面均可访问。
- R2 媒体文件的认证上传、公开读取、认证删除均已验证；未认证写入会被拒绝。

## 数据安全决策

- 未修改、未迁移、未绑定账户中既有 D1：`rope-registration-db`、`jump-rope-db`。
- 所有本次迁移写入仅发生在新建 D1：`rope-jump-registration-d1-20260814`。
- 原 Supabase 当前不可连接，故本次只迁移系统表结构与功能；如恢复旧数据导出，可后续单独导入新库。

## 本次流程加固

- 新增 `migrations/0002_workflow_hardening.sql`：报名运动员/个人项目规范化、状态迁移、审核任务、秩序册版本、当前版本和生成互斥锁。
- 新增 `functions/_shared/workflows.ts`：俱乐部报名、重提、删除、个人报名、分页查询、批量审核和原子秩序册生成均由服务端执行；通用 CRUD 禁止直接写入关键表。
- 前端 Store 已切换到受保护工作流 API；管理员审核只处理待审核记录，批量上限 50 条；秩序册读取当前已发布版本。
- 新增 `scripts/rope-jump-load-test.mjs`：默认离线模拟 100 队、1000 人、2400 条操作，支持固定种子与可控并发。
- 远程专用 D1 已成功应用 `0002_workflow_hardening.sql`，未触碰既有 D1/R2 资源。

## 验证结果

- 前后端 TypeScript 检查通过。
- Vite 生产构建通过，输出目录：`release-hardening-20260817`。
- 工作流静态校验通过。
- 离线 2400 操作模拟：50 并发、虚拟吞吐 1556.42 ops/s、p95 58ms；这属于逻辑/计划模拟，不等同于真实网络压测。
- 线上页面返回 200；未登录审核与秩序册读取均返回 401；公开赛事查询返回 200。
- 按参考图更新俱乐部端报名交互：默认先浏览项目列表，支持类别和个人/团队/集体筛选；点击“填报”后选择报名组别与队员；确认添加后回到项目列表继续添加；在“已填报项目”汇总页统一提交。
- 修复报名失败项误清空、切换队伍后报名数据不刷新、修改报名按钮始终禁用、教练选择“不指定”无法清空等边界问题。
- 新版已部署：<https://3c8844eb.rope-jump-registration.pages.dev>；俱乐部页和赛事 API 均返回 200，发布包无旧 Supabase 地址。
- 组别筛选顺序已修正：先选择项目和报名组别，再按当前项目类型、年龄和性别过滤运动员；不符合该组别的运动员直接隐藏，切换组别会自动清理失效的已选运动员。
- 已清理旧报名流程 UI 与关联代码：删除旧步骤条、旧“报名清单”入口、旧运动员优先分步表单、旧状态和处理函数；保留新项目列表、项目/组别选择、资格筛选队员、报名汇总与统一提交流程。
- 清理版本已完成前后端类型检查、生产构建，并部署：<https://1476fe3d.rope-jump-registration.pages.dev>；俱乐部页面与赛事 API 均返回 200。
- 按最新参考图完善填报交互：单个项目选完运动员后进入“已填报”确认页；“保存返回”保留内容并回到项目列表；“添加报名项”继续当前项目添加运动员；项目列表底部增加“已填报项目”汇总与“缴费报名”入口，缴费提交后仍停留在项目选择页；“已填报队伍”统一改为“已填报项目”语义。
- 本次改动文件：`src/sections/club/ClubRegForm.tsx`；TypeScript 检查通过，独立输出目录 `dist-validation-20260817-final` 的 Vite 生产构建通过。原 `dist` 清理因安全回收钩子失败未覆盖。
- 最新流程版本已部署到 Cloudflare Pages：<https://5e1aebea.rope-jump-registration.pages.dev>；部署使用独立产物目录 `release-deploy-20260817`，未修改既有 D1/R2 资源。
- 线上冒烟：主页 200、`/club` 200、`/api/auth/session` 未登录返回 200 且无会话；`/club.html` 按站点重写规则 308 到 `/club`。发布包未发现旧 Supabase 地址。
- 按最新参考图调整俱乐部报名页：底部固定“已填报项目 / 缴费报名”操作栏，始终覆盖在项目列表最底部；点击“已填报项目”打开当前页面内底部抽屉，展示项目、组别、运动员和删除操作，不跳转页面；抽屉内继续提供“缴费报名”。
- 固定底部栏版本已部署：<https://7d4b7ad1.rope-jump-registration.pages.dev>；`/club` 返回 200，未登录会话接口正常返回。
- 修正固定栏响应式位置：移动端改为紧贴底部菜单栏（无空隙），网页端改为紧贴浏览器底部，不再悬浮在项目卡片中间；位置修复版本已部署：<https://76ee2525.rope-jump-registration.pages.dev>。
- 完成桌面端与移动端报名 UI 优化：桌面端取消窄容器限制，改为全宽响应式两列项目卡片，填充两侧空白；赛事选择、筛选区和卡片信息层级重新整理。移动端保持单列，压缩冗余间距，优化筛选面板、项目卡片、按钮和固定底栏；报名逻辑、页面内抽屉和底部菜单保持不变。
- UI 优化版本已部署：<https://fc8609ba.rope-jump-registration.pages.dev>；`/club` 返回 200，未登录会话接口正常返回。
- 按红框范围修正电脑端“已报名项目”底部操作栏：固定栏从左侧导航栏右边开始，仅覆盖右侧主内容区域，到浏览器右边结束；移动端仍保持全屏宽度；页面内抽屉同步从主内容区开始。版本已部署：<https://ef8f0b98.rope-jump-registration.pages.dev>；`/club` 返回 200，会话接口返回 `{"data":null,"error":null}`。
- 将“缴费报名”改为“查看项目清单并缴费报名”：首次点击仅打开项目清单；清单内点击“确认项目清单并缴费报名”后才提交。提交成功后锁定当前赛事报名界面，禁止再次选项目、添加队员、删除清单或切换赛事；刷新页面会根据已有 pending/confirmed 报名自动保持锁定。版本已部署：<https://eae809ec.rope-jump-registration.pages.dev>；`/club` 返回 200，会话接口正常。
- 提交后右侧按钮改为“取消报名”：点击后通过受保护接口撤回当前赛事、当前队伍在管理员端的 pending/confirmed/rejected 报名记录，同时删除关联秩序册条目，保留页面已选项目并解除锁定，可再次确认提交。版本已部署：<https://78cbdc26.rope-jump-registration.pages.dev>；`/club` 返回 200，会话接口正常。
- 修复取消报名 `Internal server error`：撤回流程此前错误引用旧版 `order_entries` 表，已改为删除新版 `order_book_entries` 关联条目后再删除报名记录。修复版本已部署：<https://63e6b7dd.rope-jump-registration.pages.dev>；`/club` 返回 200，会话接口正常。
- 修复取消报名后项目清单归零：以“俱乐部 + 队伍 + 赛事”为键持久化临时报名项目；进入已提交状态会从管理员端报名记录重建清单，取消报名后保留清单并解除锁定，刷新页面也能恢复。版本已部署：<https://d3ab0c7b.rope-jump-registration.pages.dev>；`/club` 返回 200，会话接口正常。
- 修复管理员端“va.athletes.map is not a function”：管理员分页报名接口现在会将 D1 中的 `athletes` JSON 字段解码为数组，前端映射层也会兼容 JSON 文本、非法值和历史异常值，避免页面因单条数据格式异常而崩溃。版本已部署：<https://ff8f6a6b.rope-jump-registration.pages.dev>；`/admin` 返回 200，会话接口正常。
- 按要求移除俱乐部端“取消报名”入口：报名提交后右侧按钮恢复为不可点击的“报名已提交”，保留当前赛事报名锁定，俱乐部不可撤回或再次编辑已提交报名。版本已部署：<https://84b96944.rope-jump-registration.pages.dev>；`/club` 返回 200，会话接口正常。
- 俱乐部报名改为自动确认：新提交记录直接写入 `confirmed` 状态并记录系统自动确认轨迹；后台“俱乐部报名审核”改为只读的“俱乐部报名清单”，保留按队伍/全部列表、搜索、项目筛选、详情和 Excel 导出，移除确认、拒绝、批量审核与状态列。专用 D1 中原有 3 条 pending 俱乐部报名已迁移为 confirmed，个人报名审核未改动。版本已部署：<https://96628c8f.rope-jump-registration.pages.dev>；`/admin`、`/club` 返回 200。
- 新增管理员“取消报名界面锁定”功能：在俱乐部报名清单的每个队伍条目右侧可解锁该队伍报名；俱乐部端会显示“管理员已允许修改报名项目”，可修改项目和运动员。再次确认后，服务端会校验完整新清单、删除同队伍旧报名并写入修改后的自动确认清单，随后恢复锁定；管理员清单每 10 秒刷新并会标记“允许修改中”。同步标记当前秩序册为过期，需重新生成。新增专用 D1 迁移 `0003_admin_registration_edit_unlocks.sql`。
- 修复修改清单按钮卡在“提交中”：成功替换后不再等待额外数据刷新；网络或服务端异常会立即结束提交状态，并在清单内展示错误信息。该修复已发布到正式域名 <https://www.dztsbmxt.top>，确认 `/club` 与 `/admin` 返回 200。
- 修复 `competitionId is required`：管理员解锁后的替换清单此前仅在外层携带赛事 ID，清单项逐项解析时字段缺失。客户端现在逐项携带 `competitionId`，服务端同时按外层赛事/队伍作用域归一化清单项，并增加提交前赛事存在校验；未触碰任何 D1/R2 数据。修复版已部署到 Pages 预览地址 <https://99895074.rope-jump-registration.pages.dev>，正式域名 `/club`、`/admin` 均返回 200。
- 简化俱乐部端“我的报名”页面：移除报名总数、已确认、审核中统计卡和项目状态标签，统一展示项目、组别、参赛运动员、教练、报名日期的已提交项目清单；导出报名表同步移除审核状态列，不改变现有报名数据或自动确认逻辑。新版本已发布至 <https://d2554a9b.rope-jump-registration.pages.dev>，正式 `/club` 返回 200。
- “我的报名”新增两种清单视图：默认“按项目查看”保留每个项目的完整报名信息；“按运动员查看”将同一运动员的已提交项目归并为一张卡片，逐项显示项目、组别及教练，便于队伍核对个人报名。版本已发布至 <https://bb7259dc.rope-jump-registration.pages.dev>，正式 `/club` 返回 200。
- 优化“按项目查看”的移动端密度：由大卡片改为紧凑单行清单，默认一行按“项目 · 组别 · 运动员 · 日期”显示；仅在教练、出场序号或异常说明存在时增加补充行，显著减少空白区域。版本已发布至 <https://61d5364c.rope-jump-registration.pages.dev>，正式 `/club` 返回 200。
- 为测试账号 `001w` 的“德州御绳道花样跳绳俱乐部”队伍补齐测试资料：共 15 名运动员（姓名、性别、出生日期、身份证号、电话、头像均已填充，其中 14 张为本地生成的虚拟矢量头像）、2 名教练和 1 名领队；已通过俱乐部会话读取接口复核数量及字段完整性。
- 已按测试需要将账号 `001w` 当前队伍在“2026年德州市跳绳比赛”下的 2 条已提交俱乐部报名记录清除，并确认报名界面未处于管理员解锁修改态；队伍人员资料完整保留（15 名运动员及头像、2 名教练、1 名领队），可从在线报名页重新完成全流程测试。
- 修复俱乐部端限报规则：原服务端将所有项目按 `max_team_events` 混合统计，造成个人项目最多 3 项未强制执行。现增加三层保护：添加报名项时即时拦截、提交前清单复核、服务端按 `events.is_individual` 分别统计个人/集体项目并使用对应上限；管理员解锁的整单替换也加入同一清单预留计数。已在正式 API 回归验证周嘉怡：前三项个人项目可创建，第 4 项返回 409 `INDIVIDUAL_EVENT_QUOTA_EXCEEDED`，随后已清理测试记录。发布版本：<https://a85e20ad.rope-jump-registration.pages.dev>。
- 报名队员选择改为主动禁用：按当前临时清单实时统计，运动员个人项目达到 3 项或集体项目达到 4 项后，在后续同类型项目的队员列表中会直接置灰且不可点击，复选框不会被选中，并明确显示“个人/集体项目已报满”。提交前及服务端限额校验仍保留。发布版本：<https://013d4dea.rope-jump-registration.pages.dev>。
- 修复集体项目分组资格筛选：`4×30秒单摇接力` 等集体项目此前跨组逻辑错误地要求运动员年龄不小于分组最大年龄，导致少年甲组女子组（13–15 岁）漏掉 13、14 岁队员，出现同名分组“7人/4人”不一致。现改为“达到分组最小年龄即可；本组年龄与年龄更大的运动员均可选，性别严格匹配”，并同步服务端报名资格校验。发布版本：<https://d4442d14.rope-jump-registration.pages.dev>。
- 精简在线报名的项目类型筛选：移除“团队”，只保留“全部 / 个人 / 集体”；所有非个人项目统一归入“集体”筛选，报名完成确认页中的项目类型标签也同步使用“集体”。不修改项目数据或报名规则。发布版本：<https://bc452c9c.rope-jump-registration.pages.dev>。
- 优化“我的报名 → 按项目查看”的移动端文字排版：取消项目、组别、运动员姓名的省略截断；项目与组别可按内容换行，运动员名单独立完整显示，日期在窄屏时自动换到下一行、宽屏时保持右侧对齐。发布版本：<https://d93dec11.rope-jump-registration.pages.dev>。
- “我的报名 → 按项目查看”改为项目归并：同一项目只显示一个独立框，框头展示项目名与报名项数量，框内逐条列出各报名组别、运动员、日期及补充信息，避免同名项目分散重复。发布版本：<https://1c8223d0.rope-jump-registration.pages.dev>。
- 管理端将“秩序册”统一更名为“出场顺序”，涵盖侧栏、概览引导、首页、团队提示和错误提示。出场顺序生成规则改为按项目切换场次：每个新项目无论前一项目有多少条记录，均从下一场次的 1 号场地开始；例如项目 A 有 3 条时为 `1-1` 至 `1-3`，项目 B 直接从 `2-1` 开始。发布版本：<https://dd34a52d.rope-jump-registration.pages.dev>。
- 出场顺序分组调整为“年龄分组在前、性别分组在后”：页面、Excel 导出、Excel/PDF 预览的列顺序均改为项目、年龄分组、性别分组、场次、单位、运动员；重新生成时服务端按项目 → 实际最小/最大年龄 → 性别（男、女、混合）排序，避免受分组录入顺序影响。发布版本：<https://bc15436f.rope-jump-registration.pages.dev>。
- 加强出场顺序表格边界：在线页面、Excel 预览及导出的 Excel 文件均使用黑色细实线单元格边框，包含表头、数据格及合并单元格，提升投屏和打印时的可读性。发布版本：<https://b27865b1.rope-jump-registration.pages.dev>。

## 后续事项

1. 新库初始管理员为 `admin / ropejump2024`，登录后应立刻通过管理员账号管理功能重置为专属高强度密码。
2. 正式开放前建议使用真实测试账号、赛事、项目、分组和运动员 ID 做一次小批量线上写入演练；当前压测脚本默认不会写远程数据。
3. 若需保留旧报名数据，请提供 Supabase SQL/CSV/JSON 导出文件，再执行一次受控数据导入。

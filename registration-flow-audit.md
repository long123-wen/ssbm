# 报名系统流程逻辑审查

审查范围：赛事配置、俱乐部/个人报名、容量与限额、审核、重提、秩序册、导出。结论基于当前 D1 / Pages Functions 源码静态审查。

## 总结

系统的页面流程与基础数据隔离已具备可用基础，但报名的最终裁决仍有相当部分依赖浏览器端。对于 100 队、1000 人及多项目报名，建议正式开放前完成 P0 项，否则会出现审核绕过、截止绕过、容量漂移与秩序册不一致风险。

## P0：必须在正式开放前修复

### 1. 俱乐部可绕过界面直接篡改本队报名

- 当前通用 `/api/data/registrations/query` 允许俱乐部更新本队报名的 `status`、`reject_reason`、`start_order`、`bib_number` 等字段。
- 因而可绕过管理员审核，将报名自行改为已通过，甚至伪造号码布或修改已审核数据。
- 关键位置：`functions/_shared/db.ts` 的 registrations 字段白名单与 club 通用更新权限。

**修复：** 停止给俱乐部开放 registrations 通用更新。改为专用服务端动作：新建报名、拒绝后重提、待审撤回、管理员审核、秩序册编号。俱乐部绝不能写审核状态、拒绝原因、出场序号或号码布。

### 2. 俱乐部报名可绕过赛事开放与截止时间

- 当前俱乐部端的赛事状态和倒计时主要是前端判断；服务端插入报名时没有核验赛事是否 `open` 或是否超过 `registration_deadline`。
- 已截止或未开放的赛事可被直接调用 API 报名。

**修复：** 在服务端提交和重提报名时，以服务端时间校验 `competitions.status = 'open'` 且未超过截止时间；管理员补录单独走带审计的例外接口。

### 3. 运动员、项目限额与重复参赛未在服务端最终校验

- `registrations.athletes` 是 JSON，当前服务端不验证运动员是否属于当前俱乐部、队伍与赛事。
- 也不能可靠防止同一人重复报同一项目、超出个人限项、提交人数超过项目人数、绕过队伍/项目限额。

**修复：** 将报名中的运动员关联规范化为 `registration_athletes` 表；在一次服务端事务内校验运动员归属、项目人数、性别/年龄/分组、个人限项、重复项目和容量。JSON 只作为展示快照，不作为规则事实源。

### 4. 个人报名项目选择未在服务端做完整规则校验

- 个人报名虽然已校验通道开关、时间和身份证唯一性，但 `events` 仍是未验证 JSON。
- 可提交不存在项目、错误分组、超限项目或已满分组。

**修复：** 建立 `individual_registration_events` 关联表；服务端在事务内校验项目/分组归属、年龄性别、个人限项、重复选择与容量。

### 5. 秩序册生成不是原子操作

- 现有流程先删旧 `order_entries`，逐条更新报名出场编号，最后再批量插入秩序册。
- 中途断网、超时、重复点击或两个管理员并发生成时，可能留下空秩序册、半写入号码布或报名与秩序册不一致。

**修复：** 将生成移到单一服务端命令，加入 `orderbook_runs` 版本与互斥锁；先生成完整快照，再以一个 D1 事务/批次原子发布。`order_entries` 增加 `registration_id`，并对赛事内报名和号码布建立唯一约束。

## P1：建议在压测前修复

### 1. 换组重提会重复调整分组人数

- 数据库触发器已在 `group_id` 变更时更新人数；前端重提后又手动加减计数。
- 会造成 `current_count` 逐步偏离真实报名数，出现虚假满额或超额。

**修复：** 删除前端加减人数逻辑；由 D1 触发器或服务端事务作为唯一计数来源。并为换组补充目标分组容量检查。

### 2. 被拒绝报名仍可能占用名额

- 当前插入报名即占用分组计数，审核拒绝时没有释放。
- 但前端限额统计只把待审和已通过视为占用，语义不一致。

**修复：** 明确规则：通常 `pending + confirmed` 占位、`rejected` 不占位；通过状态转换触发器统一增减计数。

### 3. 批量审核可覆盖已处理记录

- 批量通过/拒绝未统一限定 `status='pending'`，可能误将已经拒绝或确认的数据覆盖。
- 当前逐条 `Promise.all` 也不适合 1000 条记录，部分失败时无法清楚恢复。

**修复：** 建立服务端批量审核接口，按 100-200 条批处理，使用 `WHERE status='pending'` 条件更新，并返回更新、跳过和冲突明细。

### 4. 审核后秩序册不会自动标记过期

- 生成秩序册后若继续确认、拒绝、重提或删除报名，旧秩序册仍可导出。

**修复：** 生成时保存确认报名快照哈希；任何影响已通过报名集的操作都标记秩序册为 stale，导出前要求重新生成。

### 5. 查询单次上限为 1000 行且没有分页

- 1000 名运动员平均报名 2 个项目就可能产生 2000 条报名记录。
- 审核、统计、导出或重复检测可能只看到前 1000 行。

**修复：** 增加服务端 cursor/offset 分页；统计和限额使用 `COUNT/EXISTS` 专用查询，导出采用分页或后台任务。

### 6. 快速新建运动员缺少当前赛事 ID

- 报名页快速新建路径可能没有传 `competitionId`，与 D1 的 `athletes.competition_id NOT NULL` 冲突。

**修复：** 快速创建时强制传当前赛事 ID，并将 store 输入类型中的 `competitionId` 设为必填。

## P2：体验与可追溯性优化

- 个人报名被拒后没有自助修订重提，只能管理员永久删除后重新报名，丢失审核历史。
- 个人报名预查重请求没有匿名读取权限，最终由唯一键拦截，用户会到提交时才发现重复。
- 赛事日期、截止日期、个人报名窗口缺少统一服务端顺序校验。
- 批量导入运动员只做了较弱校验，应校验身份证、出生日期、性别与文件内重复。
- 管理端按 `clubName` 分组；同名单位可能合并或批量误操作，应使用 `clubId`。
- 秩序册定义了场次/场地字段，但生成时没有全部持久化，读取时从号码布反解析，不利于后续改规则。
- 报名表缺少完整审核历史（审核人、审核时间、旧状态、新状态、原因），不利于争议追溯。

## 已具备的优点

- 个人报名的身份证重复最终由数据库唯一约束兜底。
- 个人报名的开放状态与时间窗口已经在服务端校验。
- 赛事、项目、分组、队伍之间已有基础外键和赛事隔离。
- 分组首次插入容量检查已放在 D1 触发器，不仅依赖前端。
- 俱乐部基础数据查询已按会话 `club_id` 做所有权隔离。
- R2 图片上传/读取/删除权限已接入并完成线上验证。

## 建议上线顺序

1. 先完成 P0-1 至 P0-5：专用报名/审核接口、服务端截止与资格校验、规范化报名人员关联、原子秩序册。
2. 再修 P1：计数一致性、批量审核、分页、过期秩序册。
3. 然后进行 100 队、1000 人、约 2000-3000 条项目报名的并发压测与恢复演练。
